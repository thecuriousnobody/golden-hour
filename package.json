{
  "name": "golden-hour-android",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor wrapper that packages the web/ Next.js build as an Android app.",
  "scripts": {
    "build:web": "cd web && CAPACITOR_BUILD=1 npm run build",
    "sync": "npm run build:web && npx cap sync android",
    "android": "npm run sync && npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.5"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.5",
    "@capacitor/core": "^7.6.5",
    "@capacitor/geolocation": "^7.1.8"
  }
}
